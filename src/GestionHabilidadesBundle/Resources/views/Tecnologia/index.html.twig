{% extends '::layout.html.twig' %}

{% block body -%}
    <div class="container">
        <div class="col-md-12">
            <div class="margin-none">
                <h1>HardSkills</h1>
            </div>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Cod ID</th>
                            <th>Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td><a href="{{ path('tecnologia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                            <td>{{ entity.nombre }}</td>
                            <td>
                            
                                    <a href="{{ path('tecnologia_show', { 'id': entity.id }) }}" class="btn btn-info">show</a>
                             
                                    <a href="{{ path('tecnologia_edit', { 'id': entity.id }) }}" class="btn btn-warning">edit</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

                    <a href="{{ path('tecnologia_new') }}" class="btn btn-info">Create new aptitud</a>
                    </li>
                </ul>
        </div>
    </div>
    {% endblock %}
