{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Mes creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('mes') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
