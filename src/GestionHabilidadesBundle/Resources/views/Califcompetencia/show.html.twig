{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Califcompetencia</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nota</th>
                <td>{{ entity.nota }}</td>
            </tr>
            <tr>
                <th>Actividad</th>
                <td>{{ entity.actividad }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('califcompetencia') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('califcompetencia_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
