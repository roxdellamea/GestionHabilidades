{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Requerido creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('requerido') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
